node_modules/
.next/
out/
dist/
.turbo/
.vercel/

# secrets — never commit
.env
.env.*
.env.all
!.env.example

.DS_Store
*.log
npm-debug.log*

# Next.js build cache + auto-generated agent rules
tsconfig.tsbuildinfo
/AGENTS.md
/CLAUDE.md

# NOTE: .agent-logs/ is intentionally NOT ignored.
# It is part of the 8x submission and ships with the repo, publicly.
.vercel

# Internal planning docs — kept local, not shipped in the public repo.
/docs/plans/
